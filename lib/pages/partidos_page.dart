import 'package:flutter/material.dart';

class PartidosPage extends StatelessWidget {
   
  const PartidosPage({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('PartidosPage'),
      ),
    );
  }
}